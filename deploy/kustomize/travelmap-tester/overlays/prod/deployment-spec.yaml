apiVersion: apps/v1
kind: Deployment
metadata:
  name: travelmap-tester
spec:
  template:
    spec:
      containers:
        - name: travelmap-tester-2222
          resources:
            requests:
              memory: "150Mi"
              cpu: "150m"
            limits:
              memory: "250Mi"
              cpu: "250m"
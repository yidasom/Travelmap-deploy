apiVersion: apps/v1
kind: Deployment
metadata:
  name: travelmap-tester
spec:
  template:
    spec:
      containers:
        - name: travelmap-tester
          resources:
            requests:
              memory: "100Mi"
              cpu: "100m"
            limits:
              memory: "200Mi"
              cpu: "200m"